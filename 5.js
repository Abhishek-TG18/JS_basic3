
function calculateCircleArea(radius) {
    return Math.PI * radius * radius;
}


let circleRadius = 5;
let circleArea = calculateCircleArea(circleRadius);
console.log("The area of the circle with radius", circleRadius, "is:", circleArea);
